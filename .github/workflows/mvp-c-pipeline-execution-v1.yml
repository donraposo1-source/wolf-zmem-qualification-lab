name: mvp-c-pipeline-execution-v1
on:
  push:
    branches: [harvest/mvp-c-pipeline-execution-v1]
  pull_request:
    branches: [main]
permissions: {}
jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Execute deterministic pipeline
        shell: bash
        run: |
          set -euo pipefail
          cd harvest/mvp-c-pipeline-execution-v1
          node --test pipeline.test.mjs 2>&1 | tee /tmp/pipeline.log
          sha256sum /tmp/pipeline.log | sed 's/^/RESULT_SHA256=/'
      - name: Cleanup
        if: always()
        run: |
          rm -f /tmp/pipeline.log
          echo CLEANUP=PASS
