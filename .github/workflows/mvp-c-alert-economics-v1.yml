name: mvp-c-alert-economics-v1
on:
  push:
    branches: [harvest/mvp-c-alert-economics-v1]
  pull_request:
    branches: [main]
permissions: {}
jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Verify alert economics
        shell: bash
        run: |
          set -euo pipefail
          cd harvest/mvp-c-alert-economics-v1
          node --test economics.test.mjs 2>&1 | tee /tmp/economics.log
          sha256sum /tmp/economics.log | sed 's/^/RESULT_SHA256=/'
      - name: Cleanup
        if: always()
        run: |
          rm -f /tmp/economics.log
          echo CLEANUP=PASS
